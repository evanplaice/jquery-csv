{
  "name": "jquery-csv",
  "version": "0.8.10",
  "description": "A jQuery CSV parser plugin. Battle Tested | Optimized | 100% IETF RFC 4180 Complete",
  "author": {
    "name": "Evan Plaice",
    "email": "evan.plaice@gmail.com"
  },
  "devDependencies": {
    "jquery": "^3.0",
    "tap-spec": "^5.0.0",
    "tape": "^4.8.0"
  },
  "main": "src/jquery.csv.js",
  "scripts": {
    "test": "npx tape ./test/*.js | tap-spec",
    "lint": "npm run lint:htmlhint && npm run lint:semistandard",
    "lint:htmlhint": "npx htmlhint --config .htmlhint examples/**/*.html",
    "lint:semistandard": "npx semistandard",
    "minify": "npm run minify:prepare && npm run minify:add && npm run minify:commit",
    "minify:prepare": "npx uglify-js src/jquery.csv.js -o src/jquery.csv.min.js --compress --mangle", 
    "minify:add": "git add src/jquery.csv.min.js",
    "minify:commit": "git commit -m 'Update minified version'",
    "preversion": "npm run test && npm run minify",
    "postversion": "git push --follow-tags",
    "prepublish": "npm lint && npm run test"
  },
  "license": "MIT",
  "repository": "http://github.com/evanplaice/jquery-csv/",
  "semistandard": {
    "globals": [
      "$",
      "jQuery"
    ]
  }
}
